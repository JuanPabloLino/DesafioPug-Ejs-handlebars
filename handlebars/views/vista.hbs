<style>
    .table td,
    .table th {
        vertical-align: middle;
    }

    h1 {
        color: rgb(30, 201, 15);
    }

    hr {
        background-color: #ddd;
    }

    .jumbotron {
        min-height: 100vh;
    }
</style>

<div class="jumbotron">
    <h1>Vista de Productos</h1>
    <br>

    {{#if hayProductos}}
    <div class="table-responsive">
        <table class="table table-dark">
            <tr>
                <th>Nombre de producto</th>
                <th>Precio de producto</th>
                <th>Foto de producto</th>
            </tr>
            {{#each productos}}
            <tr>
                <td>{{this.title}}</td>
                <td>${{this.price}}</td>
                <td><img width="50" src={{this.thumbnail}} alt="not found"></td>
            </tr>
            {{/each}}
        </table>
    </div>
    {{else}}
    <h3 class="alert alert-warning">No se encontraron los productos</h3>
    {{/if}}
    <br>
    <a href="/" type="button" class="btn btn-primary">Añadir un nuevo producto</a>
    
